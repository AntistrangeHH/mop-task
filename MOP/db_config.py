# I will separate db info into separate script, so it's easier to change db settings

db_config = {
    'name' : 'rss_items',
    'user' : 'postgres',
    'password' : 'postgres',
    'host': 'localhost',
    'port': 5432,
}